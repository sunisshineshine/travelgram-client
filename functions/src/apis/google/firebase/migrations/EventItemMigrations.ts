export const returnEventItem = async (
  target: EventItem
): Promise<EventItem> => {
  return target;
};
export const returnEventItems = async (
  target: EventItem[]
): Promise<EventItem[]> => {
  return target;
};

// 20201005 initialized
