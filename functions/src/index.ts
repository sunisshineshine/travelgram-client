import * as PLANS from "./apis/google/firebase/plans";
import * as PLACES from "./apis/google/maps/places";

export const { placeDetail, placeAutocompletion } = PLACES;

export const { plan, plans, createPlan } = PLANS;
